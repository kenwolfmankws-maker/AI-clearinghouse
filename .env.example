# Copy to .env and fill in your real key (never commit .env)

# Option A: Direct OpenAI (default)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY

# Option B: Vercel AI Gateway (unified provider endpoint)
# If set, serverless function will use AI Gateway with baseURL https://ai-gateway.vercel.sh/v1
# and model names like openai/gpt-4o-mini or anthropic/claude-sonnet-4
AI_GATEWAY_API_KEY=YOUR_AI_GATEWAY_API_KEY

# Optional: override default chat model
# For AI Gateway include provider prefix, for OpenAI omit prefix
# CHAT_MODEL=openai/gpt-4o-mini

# OIDC (Vercel Authentication to your own API)
# Team slug for issuer URL (recommended)
VERCEL_TEAM_SLUG=your-team-slug
# Expected audience to verify in tokens
OIDC_AUDIENCE=https://vercel.com/your-team-slug
# Optional: enforce a specific subject value
# OIDC_SUBJECT=owner:your-team-slug:project:your-project:environment:production

# Require OIDC for chat API (set to true to enforce Authorization: Bearer <token>)
# REQUIRE_OIDC_FOR_CHAT=false
